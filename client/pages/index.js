import Layout from "../components/Layout";

// note - Module name is very important in 'pages' dir since this will be a page route name
const Home = () => {
  return <Layout> Main</Layout>;
};

export default Home;
